<div class="field">
    <fieldset>
        {{#if label}}<legend>{{label}}</legend>{{/if}}
        <input type="{{date_type}}" name="{{name}}" value="{{value}}" autocomplete="off">
    </fieldset>
</div>